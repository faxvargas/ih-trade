<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List</title>
    <link rel="stylesheet"  href="/stylesheets/styles.css"/>
</head>
<body>
    {{#if PostList}}
{{#each PostList}}
<div class="list-group">
<a href="/post/{{this.id}}/edit" class="list-group-item list-group-item-action">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">{{nombreDelProducto}}</h5>
      <small class="text-muted">{{fechaDePublicacion}}</small>
      <small class="text-muted">{{NumeroTelefonico}}</small>

      <form action="/post/{{this.id}}/delete" method="POST">
 <button type="submit" class="btn btn-secondary btn-sm">Delete</button>
</form>

    </div>
    <p class="mb-1">{{descripcion}}.</p>
    <small class="text-muted">{{razonDeVenta}}.</small>
    
  </a>
  </div>    
 {{/each}}
 {{/if}}

 <button type="submit" class="btn btn-secondary btn-sm"><a href="/post/create">Create a new post</a></button>
</body>
</html>
